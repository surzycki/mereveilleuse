:javascript
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

  ga('create', "#{ENV['GOOGLE_ANALYTICS_TRACKING_ID']}", 'auto');
  ga('send', 'displayfeatures')
  
  // set cookie to send to server after ga load
  // so the server side events know where they are coming from
  ga(function(tracker) {
    var clientId = tracker.get('clientId');
    updateCookie({'ga_clientId': clientId }, 'mereveilleuse-analytics')
  }); 
